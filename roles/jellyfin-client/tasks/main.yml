- name: Update repo cache and install chromium
  apt:
    name: chromium-browser
    update_cache: yes
  become: true

- name: Add jellyfin.sh script
  synchronize:
    src: jellyfin-client.sh
    dest: /usr/local/bin/jellyfin-client.sh
  become: true
